//Multidimensional Array in Dynamic

#include<stdio.h>
int main()
{
    int a[10][10],i,j,row,col;
    printf("\n Enter row and col value");
    scanf("%d%d",&row,&col);  //3 ,3
    printf("Enter the elements into array");
    for(i=0;i<row;i++)   //Go for each row
    {
        for(j=0;j<col;j++)  // Go for each col
        {
            scanf("%d",&a[i][j]);
        }
    }
    printf("The elements in Matrix is:\n");
    for(i=0;i<row;i++)   //checking for each row
    {
        for(j=0;j<col;j++)  // checking for each col
        {
            printf("%d ",a[i][j]);
        }
    
    printf("\n");
}
}
output
Enter row and col value3 3
Enter the elements into array1 2 3 4 5 6 7 8 9
The elements in Matrix is:
1 2 3 
4 5 6 
7 8 9 
