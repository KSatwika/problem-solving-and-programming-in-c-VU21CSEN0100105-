#include<stdio.h>
#include<string.h>
int main()
{
   char a[10];
   int len=0,i;
   printf("Enter the string"); //Gitam
   gets(a);
  for(i=0;a[i]!=0;i++)// checking string values
  {
    len=len+1;  
  }
  printf("length of string is %d",len);
  return 0;
}
